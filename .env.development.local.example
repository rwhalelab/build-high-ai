# ============================================
# 로컬 개발 환경 전용 설정 예제 (.env.development.local.example)
# ============================================
# 이 파일을 복사하여 .env.development.local 파일을 생성하세요.
# Next.js 우선순위: .env.development.local > .env.local > .env.development
# Git에 커밋되지 않습니다 (.gitignore에 포함됨)

# NEXTAUTH 설정 (로컬 개발용)
# 로컬 환경에서는 항상 localhost를 사용하도록 설정
NEXTAUTH_URL=http://localhost:3000
NEXT_PUBLIC_NEXTAUTH_URL=http://localhost:3000

# 참고: 다른 환경변수는 .env.local에서 상속됩니다.
# Supabase, Google OAuth, Gemini API 키 등은 .env.local에 설정하세요.
